class MarchingSquares {
  rows: number;
  cols: number;
  cells: Array<number>

  constructor(rows:number, cols: number) {
    this.rows = rows;
    this.cols = cols;
    this.cells = [];
  }

  init(grid:Array<boolean>) {
    
  }
}

export default MarchingSquares;